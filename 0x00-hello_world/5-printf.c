#include <stdio.h>

/**
 * main - has a return type which is int
 * Return: By default returns zero if no erros in your code
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art\n");

return (0);
}

